package com.builtinmedia.hris.ui.theme

import androidx.compose.ui.graphics.Color


object HrisColors {

    // ─────────────────────────────────────────────
    // Brand
    // ─────────────────────────────────────────────

    val Primary = Color(0xFF2196F3)
    val PrimaryDark = Color(0xFF1976D2)
    val PrimaryLight = Color(0xFF64B5F6)

    val Sky = Color(0xFFE3F2FD)
    val SkySoft = Color(0xFFF4FAFF)


    // ─────────────────────────────────────────────
    // Light
    // ─────────────────────────────────────────────

    val Background = Color(0xFFF8FAFC)
    val Surface = Color(0xFFFFFFFF)

    val TextPrimary = Color(0xFF0F172A)
    val TextSecondary = Color(0xFF64748B)
    val TextDisabled = Color(0xFF94A3B8)

    val Border = Color(0xFFE2E8F0)


    // ─────────────────────────────────────────────
    // Dark
    // ─────────────────────────────────────────────

    val DarkBackground = Color(0xFF0B1220)
    val DarkSurface = Color(0xFF111827)
    val DarkSurfaceVariant = Color(0xFF1E293B)

    val DarkTextPrimary = Color(0xFFF8FAFC)
    val DarkTextSecondary = Color(0xFF94A3B8)
    val DarkTextDisabled = Color(0xFF64748B)

    val DarkBorder = Color(0xFF334155)


    // ─────────────────────────────────────────────
    // Semantic
    // ─────────────────────────────────────────────

    val Success = Color(0xFF22C55E)
    val SuccessBg = Color(0xFFDCFCE7)

    val Warning = Color(0xFFF59E0B)
    val WarningBg = Color(0xFFFEF3C7)

    val Error = Color(0xFFEF4444)
    val ErrorBg = Color(0xFFFEE2E2)

    val Info = Color(0xFF2196F3)
    val InfoBg = Color(0xFFE3F2FD)


    // Dark semantic
    val SuccessLight = Color(0xFF4ADE80)
    val SuccessDarkBg = Color(0xFF14532D)

    val WarningLight = Color(0xFFFBBF24)
    val WarningDarkBg = Color(0xFF78350F)

    val ErrorLight = Color(0xFFF87171)
    val ErrorDarkBg = Color(0xFF7F1D1D)

    val InfoLight = Color(0xFF60A5FA)
    val InfoDarkBg = Color(0xFF1E3A5F)
}